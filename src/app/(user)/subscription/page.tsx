import type { NextPage } from "next";
import SubscriptionView from "@/components/view/SubscriptionView";

const Subscription: NextPage = () => {
    return <SubscriptionView />;
};

export default Subscription;
