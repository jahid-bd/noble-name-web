/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: [
      'localhost',
      'https://api.noblenames.co.uk',
      'api.noblenames.co.uk',
    ],
  },
};

export default nextConfig;
